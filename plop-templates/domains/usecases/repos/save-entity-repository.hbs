import { {{pascalCase name}} } from '@/domains/{{dashCase name}}/entities';

export interface ISave{{pascalCase name}}Repository {
  save(
    {{camelCase name}}Params: ISave{{pascalCase name}}Repository.Params,
  ): Promise<ISave{{pascalCase name}}Repository.Result>;
}

export namespace ISave{{pascalCase name}}Repository {
  export type Params = {{pascalCase name}};
  export type Result = {{pascalCase name}};
}
