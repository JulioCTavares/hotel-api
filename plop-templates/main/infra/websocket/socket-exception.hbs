import {
  DefaultException,
  ExceptionTypes,
} from '@/shared/helpers/error-helper';

export class SocketException extends DefaultException {
  constructor(error: any) {
    super({
      type: ExceptionTypes.SYSTEM,
      code: 'SOCKET',
      data: error,
    });
  }
}
