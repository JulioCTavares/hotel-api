import { Socket } from 'socket.io';

/* example
*  import { websocketEvents } from '@/your-domain/event-feature';
*/

export const addAvailableEvents = (socketClient: Socket) => {
  /*
  *  here an example of how to import listeners properly:
  *
  *  websocketEvents(socketClient);
  *
  *   add other possible domain listeners below here...
  */
};
